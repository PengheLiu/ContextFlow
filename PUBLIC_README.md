# ContextFlow · 浏览器 公开userscript

**版本：0.2.0**

把网页中的一次性阅读，变成可复用、可带走的上下文。

## 能力

- 四色高亮、批注、用户总结；可把截图或原文图片粘贴到批注、解释补充和总结的任意文字位置
- 日间 / 夜间双主题：跟随当前网页的明暗自动切换，正文标记与高亮同步换色
- 浏览器 LLMBridge 速览、翻译与解释
- 刷新后恢复锚点与记录
- 本地缓存，重复问题不重复调用 AI
- 复制或下载整篇 Markdown
- 用户主动授权后，一文一档同步到 Obsidian Vault 或普通 Markdown 文件夹
- 首次阅读日索引与 marker 幂等更新

## 安装

在 浏览器 userscript平台安装，或把 `skill.js` 完整粘贴到脚本userscript编辑器。绑定目标网页后运行。

要求：支持 CSS Custom Highlight、IndexedDB、LLMBridge 的 浏览器；文件夹同步还需要 Chromium File System Access API。

## 数据与隐私

- 高亮、批注、正文上下文、AI 结果、图片附件、目录 handle 和同步元数据保存在当前网站的浏览器 IndexedDB；界面偏好可能保存在 localStorage。
- 请求速览、翻译或解释时，完成任务所需的正文分块、选区和相关历史会通过 **浏览器 LLMBridge** 发送给 浏览器 提供的 AI 能力。
- ContextFlow 公开版不连接 ContextFlow 自有服务器，也不要求 API Key。
- 只有用户通过浏览器目录选择器主动授权后，userscript才能读写该目录；同步只维护带 `<!-- cf:... -->` 标记的 Markdown 块，并尽量保留用户手工内容。
- “忘记文件夹”只清除浏览器保存的目录 handle 和同步状态，不删除已经写入的文件。
- “复制”会写入系统剪贴板；“下载”会创建本地 Markdown 文件。
- 清理该网站的浏览器数据会清除本地阅读记录和目录授权。

## 限制

- 浏览器存储按网站 Origin 隔离；在新网站首次同步时可能需要重新选择同一文件夹。
- 单篇正文最多缓存 400,000 字符；超过时界面会提示 AI 基于截断上下文。
- 单张粘贴图片上限 8 MB，第一版支持 PNG、JPEG 与 WebP。
- LLMBridge 单次等待上限 60 秒；超时仅停止等待，平台侧计算未必能被取消。
- 公开版不包含思源、MCP、本地服务、本地 Agent 或供应商密钥配置。

## 笔记同步

1. 打开面板 → 配置。
2. 选择 Obsidian 或普通 Markdown 模式。
3. 点击“选择文件夹”，通过浏览器授权。
4. 返回文章，点击底部“同步”。

同步产生一个文章文件与一个首次阅读日索引。重复同步不会重复追加；修改后的 ContextFlow 管理块会原地更新。若用户手工改写了管理块，userscript不会静默覆盖。

## 校验

```bash
shasum -a 256 -c SHA256SUMS
```
